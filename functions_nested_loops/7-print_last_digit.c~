#include "main.h"

/**
 * print_last_digit - print the last digit of a number
 * @n, digit: character to check
 * Return: 0 if value of last digit
 */
int print_last_digit(int)
{
	int n, digit;

	if (digit == n % 10)
	{
		_putchar (digit);
		return (0);
	}

}
