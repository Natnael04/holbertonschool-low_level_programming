#include "main.h"

/**
 *main - the name of the program
 *@argc: the number of argument to pass to the string
 *@argv: argument vector pointer of the string
 *
 *Return: always 0
 */
int main(int argc, char *argv[])

{
	(void) argc;
	
	printf("program's name is: %s\n",argv[0]);
	return 0;
}
