#include "main.h"
#include <stdio.h>

/**
 * swap_int - to swaps the values of two integer
 *
 *@a : first integer to swap
 *@b : second integer to swap
 *
 * Return: Always 0.
 */
void swap_int(int *a, int *b)
{
	int num;

	num = *a;
	*a = *b;
	*b = num
}
