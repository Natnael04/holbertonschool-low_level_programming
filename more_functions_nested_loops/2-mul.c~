#include "main.h"
#include <stdio.h>

/**
 *mul - check the code to multiplies two integer
 *@a: the first number to multiply
 *@b: the second number to multiply with first number
 *
 * Return: the result is mul
 */
int mul(int a, int b)
{

	{
		return (mul);
	}

}
